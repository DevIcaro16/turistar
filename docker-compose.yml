services:
  backend-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: backend-api
    restart: unless-stopped
    ports:
      - "${PORT:-8000}:8000"
    environment:
      - NODE_ENV=production
      - HOST=0.0.0.0
      - PORT=${PORT:-8000}
      - DATABASE_URL=${DATABASE_URL}
    env_file:
      - .env
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
